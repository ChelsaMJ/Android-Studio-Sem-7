<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ClassPerformance">
        <activity
            android:name=".WebAppActivity"
            android:exported="true" />

        <activity
            android:name=".WebViewActivity"
            android:exported="true" />

        <activity
            android:name=".StateMapActivity"
            android:exported="true" />
        <activity
            android:name=".MapsActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- api for maps -->
        <!-- <meta-data -->
        <!-- android:name="com.google.android.geo.API_KEY" -->
        <!-- android:value="AIzaSyCpP4cdW1_Ma8ewvyxpVDnYC-JhmbvqBAE" /> &lt;!&ndash;shah's api&ndash;&gt; -->
        <!-- api for maps and places -->
        <!-- <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyA885Rsjovh2Lqktb8I-L-Sr9hvCyuFsMs" /> -->
    </application>


</manifest>
